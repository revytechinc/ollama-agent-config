# cloudbsd-ollama-agents — exact-match so curl gets text/plain, not HTML or octet-stream
location = /install-ollama-agents.sh {
    alias /usr/local/www/install/install-ollama-agents.sh;
    types { }
    default_type text/plain;
    charset utf-8;
    add_header X-Content-Type-Options nosniff;
    add_header Cache-Control "no-cache";
}
location = /install-ollama-agents.sh.sha256 {
    alias /usr/local/www/install/install-ollama-agents.sh.sha256;
    types { }
    default_type text/plain;
    charset utf-8;
    add_header X-Content-Type-Options nosniff;
    add_header Cache-Control "no-cache";
}
